📱 Mobile Price Prediction (Machine Learning + Streamlit)
📌 Project Overview

This project predicts the expected price of a mobile phone based on its specifications such as brand, model, RAM, storage, color, and rating.

The application uses a Machine Learning model trained with Scikit-learn and provides an interactive web interface built with Streamlit.

Users can select mobile specifications and instantly get a predicted price.
